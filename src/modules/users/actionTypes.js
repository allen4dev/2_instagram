import { NAME } from './constants';

export const ADD_USER = `${NAME}/ADD_USER`;
export const ADD_USERS = `${NAME}/ADD_USERS`;

export const SET_CURRENT_USER = `${NAME}/SET_CURRENT_USER`;
export const ADD_USER_PHOTO = `${NAME}/ADD_USER_PHOTO`;
export const ADD_USER_PHOTOS = `${NAME}/ADD_USER_PHOTOS`;
