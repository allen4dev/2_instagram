// @flow

// Normally you will use this file fow types (typescripr, flow)
// and utility functions for the feature

// Actions

export type Comment = {
  avatar: string,
  body: string,
  displayName: string,
  id: string,
  uid: string,
};
